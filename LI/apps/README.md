# Application Contracts (Deferred)

This directory is reserved for platform-specific application contracts
that implement the canonical runtime loop defined in:

`LI/runtime/core_loop.md`

No application contracts are currently authoritative.

This directory is intentionally minimal until runtime semantics stabilize.