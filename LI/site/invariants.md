# Site LI Invariants

1. No tool may write into `LI/site/`.
2. Curated reading paths are authoritative and must not be regenerated automatically.
3. Derived artifacts are disposable and reproducible.
4. Output in `docs/` is disposable and reproducible.
5. The site build must be deterministic given the compiled manifest.
