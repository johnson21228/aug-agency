## Conversational UI Requirement

The conversational UI is a primary view over continuity.
It must support:
- resuming an ongoing thread as re-entry (not summary),
- selecting a position in continuity as context for the next message,
- switching perspective (along-path / neighborhood / global) without losing position.

The conversation view must never be the source of continuity authority.
